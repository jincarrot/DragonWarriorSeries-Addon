"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Input = void 0;
const react_1 = __importStar(require("react"));
const useSetProp_1 = require("./useSetProp");
const useSetStyle_1 = require("./useSetStyle");
const Input = (_a) => {
    var { style, className, id, value, maxLength, disabled, innerRef, type } = _a, handlers = __rest(_a, ["style", "className", "id", "value", "maxLength", "disabled", "innerRef", "type"]);
    const defaultRef = (0, react_1.useRef)(null);
    const ref = innerRef !== null && innerRef !== void 0 ? innerRef : defaultRef;
    (0, useSetProp_1.useSetProp)(className, (value) => {
        if (ref.current === null)
            return;
        ref.current.className = value !== null && value !== void 0 ? value : '';
    });
    (0, useSetProp_1.useSetProp)(id, (value) => {
        if (ref.current === null)
            return;
        ref.current.id = value !== null && value !== void 0 ? value : '';
    });
    (0, useSetProp_1.useSetProp)(value, (value) => {
        if (ref.current === null)
            return;
        const element = ref.current;
        element.value = value !== null && value !== void 0 ? value : '';
        element.setAttribute('value', value !== null && value !== void 0 ? value : '');
    });
    (0, useSetProp_1.useSetProp)(disabled, (value) => {
        if (ref.current === null)
            return;
        if (value === undefined || value === false) {
            ref.current.removeAttribute('disabled');
        }
        else {
            ref.current.setAttribute('disabled', '');
        }
    });
    (0, useSetProp_1.useSetProp)(maxLength, (value) => {
        if (ref.current === null)
            return;
        if (value !== undefined) {
            ref.current.maxLength = value;
        }
        else {
            ref.current.removeAttribute('maxlength');
        }
    });
    (0, useSetProp_1.useSetProp)(type, (value) => {
        if (ref.current === null)
            return;
        if (value !== undefined) {
            ref.current.setAttribute('type', value);
        }
        else {
            ref.current.removeAttribute('type');
        }
    });
    (0, useSetStyle_1.useSetStyle)(style, ref);
    return react_1.default.createElement("input", Object.assign({ ref: ref }, handlers));
};
exports.Input = Input;
//# sourceMappingURL=Input.js.map